# Claude Instructions

## Git Workflow

**ALWAYS commit and push directly to `main`. Never create feature branches. No exceptions.**

- All changes go to `main`
- Never use `git checkout -b`, never use feature branches
- Push with: `git push origin main`
